-- CreateTable
CREATE TABLE "Gems" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "amount" INTEGER NOT NULL
);

-- CreateTable
CREATE TABLE "Cents" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "amount" INTEGER NOT NULL
);
